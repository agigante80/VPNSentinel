# Test Requirements for VPN Sentinel Project
# Install with: pip install -r tests/requirements.txt

# Core testing framework
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-mock>=3.15.1
pytest-asyncio>=1.4.0

# HTTP testing
requests-mock>=1.12.1
pytest-httpserver>=1.1.5
requests>=2.34.2

# Flask testing
pytest-flask>=1.3.0

# Time and date mocking
freezegun>=1.5.5

# Test data generation
factory-boy>=3.3.3
faker>=40.38.0

# System monitoring (for health checks)
psutil>=7.2.2

# Coverage reporting
coverage>=7.16.0

# Code quality (optional but recommended)
flake8>=7.3.0
black>=26.5.1
# Dead-code detection (dev-only, used by scripts/find-dead-code.sh)
vulture>=2.16

# YAML parsing (used by tests/unit/test_release_workflow_guards.py to parse GitHub
# Actions workflow YAML structurally rather than by regex/hand-rolled parsing)
PyYAML>=6.0.3
